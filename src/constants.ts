// A4 at 300 DPI (full resolution for PDF export)
export const PAGE_WIDTH = 2480;
export const PAGE_HEIGHT = 3508;

// Display scale: show page at ~595px wide on screen
export const DISPLAY_SCALE = 0.24;

// A4 in PDF points (72 DPI)
export const PDF_WIDTH = 595.28;
export const PDF_HEIGHT = 841.89;

// Snap angle threshold for rotation (degrees)
export const SNAP_THRESHOLD = 5;
export const SNAP_ANGLES = [0, 90, 180, 270, 360];
